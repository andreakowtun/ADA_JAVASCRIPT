//Declara una variable de tipo string | null. 
// Asigna inicialmente un valor de cadena, 
// y luego cambia su valor a null. 
// Imprime ambos estados en la consola, 
// explicando el significado de null en TypeScript.

let cadenaNull: string | null = null;

console.log(cadenaNull);


cadenaNull='525'
console.log(cadenaNull);

console.log("Null: Representa un valor intencionalmente vacío o inexistente. Se asigna a una variable para indicar que no tiene valor o que su valor se 'vacio'.");