"use strict";
//Declara dos variables booleanas y 
// realiza una comparación lógica (como AND, OR). Imprime el resultado.
let esCasado = false;
let estaDesempleado = true;
if (esCasado && estaDesempleado) {
    console.log('consigue un empleo');
}
else if (esCasado || estaDesempleado) {
    console.log('no se que poner aca');
}
